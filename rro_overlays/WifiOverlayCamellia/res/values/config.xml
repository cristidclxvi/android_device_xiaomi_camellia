<?xml version="1.0" encoding="utf-8"?>
<!--
     SPDX-FileCopyrightText: The LineageOS Project
     SPDX-License-Identifier: Apache-2.0
-->
<resources>
    <!-- boolean indicating whether the WiFi chipset has 5GHz band support.
         Note: This config is replacing the config_wifi_dual_band_support
         since more bands may now be supported (such as 6GHz), the naming dual_band
         is no longer indicative, and a separate config now exists for each band -->
    <bool translatable="false" name="config_wifi5ghzSupport">true</bool>

    <!-- Boolean indicating whether 802.11r Fast BSS Transition is enabled on this platform -->
    <bool translatable="false" name="config_wifi_fast_bss_transition_enabled">true</bool>

    <!-- Boolean indicating whether the wifi chipset supports background scanning mechanism.
         This mechanism allows the host to remain in suspend state and the dongle to actively
         scan and wake the host when a configured SSID is detected by the dongle. This chipset
         capability can provide power savings when wifi needs to be always kept on. -->
    <bool translatable="false" name="config_wifi_background_scan_support">true</bool>

    <!-- Allowed 2.4GHz SoftAP channels. Deliberately 1-11, not 1-13.
         12 and 13 are legal in several of our SKUs' regions, and once the
         country code actually reaches the driver ACS will happily pick 12 -
         but clients sold in regions capped at 11 then cannot see the AP at
         all. Measured 2026-08-09: ACS chose channel 12 and a second phone
         could not find the network; forcing 2437 (ch 6) made it appear
         immediately. Two channels of spectrum are worth less than a hotspot
         every device can join. -->
    <string translatable="false" name="config_wifiSoftap2gChannelList">1-11</string>

    <!-- 5GHz and 6GHz SoftAP channel lists are deliberately not overridden.
         The old 5GHz list pinned 38/42/46, which are 40MHz centre channels and
         not selectable control channels, and it hid part of the band on the
         SKUs that allow it. Empty (the default) means "whatever the regulatory
         domain permits", which is what a five-SKU global device wants. There is
         no 6GHz radio here at all - this is 1x1 Wi-Fi 5. -->

    <!-- Wifi driver supports Automatic channel selection (ACS) for softap -->
    <bool translatable="false" name="config_wifi_softap_acs_supported">true</bool>

    <!-- Wifi driver supports WPA3 Simultaneous Authentication of Equals (WPA3-SAE) for softap -->
    <bool translatable="false" name="config_wifi_softap_sae_supported">true</bool>

    <!-- Wifi driver supports IEEE80211AC for softap -->
    <bool translatable="false" name="config_wifi_softap_ieee80211ac_supported">true</bool>

    <!-- Indicates that connected MAC randomization is supported on this device -->
    <bool translatable="false" name="config_wifi_connected_mac_randomization_supported">true</bool>

    <!-- Indicates that p2p MAC randomization is supported on this device -->
    <bool translatable="false" name="config_wifi_p2p_mac_randomization_supported">true</bool>

    <!-- Configure wifi tcp buffersizes in the form:
         rmem_min,rmem_def,rmem_max,wmem_min,wmem_def,wmem_max -->
    <string name="config_wifi_tcp_buffers" translatable="false">1730560,3461120,6922240,524288,1048576,4525824</string>

    <!-- integer indicating additional disconnect delay (in ms) after IMS onLost() indication is received -->
    <integer translatable="false" name="config_wifiDelayDisconnectOnImsLostMs">1500</integer>

    <!-- Indicates that SAE Hash-to-Element is supported on this device -->
    <bool translatable="false" name="config_wifiSaeH2eSupported">true</bool>

    <!-- Push the country code to the STA even while it is associated.
         Without this, WifiCountryCode only applies the country when no client
         mode manager is mid-connection, so on a device that associates during
         boot and stays associated the telephony country is never applied at
         all: the driver keeps reporting a null country and the firmware runs
         the worldwide row of its TX power table, which costs range. gen4m uses
         REGULATORY_CUSTOM_REG, so `iw reg get` still reads "country 00" even
         once this works - check mDriverCountryCode in `dumpsys wifi` instead.

         config_wifiForcedSoftApRestartWhenCountryCodeChanged is deliberately
         left at its default of true: if the regulatory domain really does
         change, a running softap may be sitting on a channel that is no longer
         permitted, and restarting it is the correct response. -->
    <bool translatable="false" name="config_wifiStaDynamicCountryCodeUpdateSupported">true</bool>
</resources>
